# 2026-05-02 前端跟进合并 Handoff

## 文档说明
本文件合并了以下两份 2026-05-02 handoff，并作为后续唯一参考：
- `2026-05-02-frontend-followup-handoff-round2.md`
- `2026-05-02-frontend-save-feedback-handoff.md`

## 已完成（状态更新）
1. 保存确认弹窗已落地，且风格与现有前端一致。
2. 弹窗已展示章节状态与关键变更路径摘要（非完整字段级 diff）。
3. 载荷区已新增 AW2 输入（动车/拖车），并接入回填、保存与运行链路。
4. 主页“打开既有项目”主链路可进入只读总览并加载配置。

## 必须保持的导航约束（硬限制）
1. 从主页不能直接跳回配置工作台。
2. 只能通过以下路径进入配置工作台：
   `主页 -> 打开既有项目 -> 只读总览 -> 修订/进入配置工作台`
3. 从配置工作台返回主页时，若有未保存改动，必须提示确认；确认离开后未保存改动丢弃。

## 当前遗留项
1. JSON 联动高亮“定位精度”仍不稳定：
- 红字高亮路径正确；
- 但自动滚动定位可能偏移；
- 需要改为“基于解析树路径到渲染行号”的确定性映射，替换当前弱匹配策略。

2. 保存确认弹窗信息深度仍可增强：
- 当前只展示状态 + 路径摘要；
- 仍缺“保存前字段级前后值对比（before/after）”。

3. 保存结果可见反馈仍建议补强：
- 建议增加 `saving/success/error` 明确状态；
- 成功提示可带版本号或时间戳，降低重复点击与不确定性。

## 风险与注意
1. 任何前端防错处理不得改变 V1 输入契约含义。
2. 不得改 specs，不得发明与后端契约不一致的字段结构。
3. JSON 定位修复应仅影响右侧 JSON 面板，不得联动滚动主输入区。

## 建议回归清单
1. 新建项目填 AW0/AW2/AW3 与 bogie_weight，保存后重开并运行。
2. 验证“主页受限路径”：只能从“打开既有 -> 只读总览 -> 修订”回到配置台。
3. 验证未保存离开提示与丢弃行为一致。
4. 验证 YAML 高亮与 JSON 高亮路径一致，记录 JSON 定位偏差场景。
